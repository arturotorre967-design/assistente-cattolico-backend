services:
  - type: web
    name: assistente-cattolico-backend
    env: python3.11
    rootDir: /
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
